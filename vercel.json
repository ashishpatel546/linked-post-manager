{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/build-public.mjs",
  "outputDirectory": "public",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "comment": "Without this only /api/<one-segment> reaches the function. On a project with no framework preset, the catch-all filename alone does not route nested paths — /api/drafts worked while /api/drafts/<id>, /api/drafts/<id>/publish and /api/research/images all returned Vercel's own 404. Publishing was among the casualties.",
      "source": "/api/:path*",
      "destination": "/api/[...path]"
    }
  ],
  "headers": [
    {
      "source": "/sw.js",
      "headers": [{ "key": "cache-control", "value": "no-store" }]
    },
    {
      "source": "/index.html",
      "headers": [{ "key": "cache-control", "value": "no-store" }]
    }
  ]
}
